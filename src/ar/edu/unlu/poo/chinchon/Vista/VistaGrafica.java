package ar.edu.unlu.poo.chinchon.Vista;

import ar.edu.unlu.poo.chinchon.Modelo.JugadorMostrable;

import javax.swing.*;
import java.awt.*;
import java.util.ArrayList;

public class VistaGrafica {
    /*public void mostrarPuntos(ArrayList<JugadorMostrable> jugadores){
        JDialog dialogo = new JDialog(this,"Puntos Jugadores",false);
        dialogo.setSize(300, 200);
        JPanel panelContenido = new JPanel();
        panelContenido.setLayout(new BoxLayout(panelContenido, BoxLayout.Y_AXIS));
        panelContenido.add(new JLabel("RONDA Nº "+ (controlador.obtenerCantidadDeRondas()+1)));
        for (JugadorMostrable j:jugadores) {
            panelContenido.add(new JLabel("Jugador "+j.getNombre()+": "+ j.getPuntos()));
            panelContenido.add(Box.createRigidArea(new Dimension(0, 10)));
        }
        dialogo.add(panelContenido);
        dialogo.setVisible(true);
    }*/
}
